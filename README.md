# Simple Weather CLI

A lightweight command-line tool that fetches current weather data for any city using the Open-Meteo API.

## Features
- No API key required
- Auto-geocoding (converts city names to coordinates)
- Fast and lightweight

## How to Run
1. Clone the repo
2. Install dependencies: `pip install -r requirements.txt`
3. Run the script: `python main.py London`
